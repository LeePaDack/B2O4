<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b204.mapper.MypageMapper">
	<select id="userFind" parameterType="member">
	<!-- 마이페이지 멤버 정보 보기 -->
		select memberId, memberPw, memberEmail, memberName, memberPhone, 
					memberAddress, memberProfile
		from member
		where memberNo = #{memberNo}
	</select>
</mapper>