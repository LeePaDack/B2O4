<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="b2o4.mapper.SignupMapper">

  <select id="findAll" resultType="b2o4.dto.Signup">
    SELECT * FROM signups
  </select>

  <insert id="insertSignup" parameterType="b2o4.dto.Signup">
    INSERT INTO signups (memberId, memberPw, memberEmail, memberName, memberBirth, memberPhone, memberAddress, memberProfile)
    VALUES (#{memberId}, #{memberPw}, #{memberEmail}, #{memberName}, #{memberBirth}, #{memberPhone}, #{memberAddress}, #{memberProfile})
  </insert>

  <select id="idCheck" parameterType="String" resultType="int">
    SELECT COUNT(*) FROM signups WHERE memberId = #{memberId}
  </select>

</mapper>
