<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b204.mapper.LiveChatMessageMapper">
	<!--  
	<insert id="recordMessage">
		insert into LiveChatMessage(memberNo, msgContent, msgAt) values (#{memberNo}, #{msgContent}, now())
	</insert>
	-->
	<!--  
	<select id="showMessage">
		SELECT M.memberId, L.msgContent FROM LiveChatMessage L JOIN member M ON (L.memberNo = M.memberNo)
		WHERE L.memberNo = #{memberNo}
	</select>
	-->
	<insert id="recordMessage">
		insert into LiveChatMessage(msgContent) values (#{msgContent})
	</insert>
	<select id="showMessage" resultType="String">
		SELECT msgContent FROM LiveChatMessage 
	</select>
</mapper>